{
  "name": "@qr-bc/contracts",
  "version": "0.0.0-dev",
  "private": true,
  "description": "ProductRegistry smart contract — Foundry + Hardhat dual toolchain",
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "build:hardhat": "hardhat compile",
    "test": "forge test",
    "test:hardhat": "hardhat test",
    "lint": "solhint 'src/**/*.sol' 'test/**/*.sol' 'script/**/*.sol'",
    "clean": "forge clean && hardhat clean",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-foundry": "^1.1.3",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@qr-bc/shared": "workspace:*",
    "@types/node": "^20.16.10",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "hardhat": "^3.4.5",
    "solhint": "^5.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
